# ---------------------------------
# 1. CORE & DEPENDENCIES
# ---------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
package-lock.json # Opsional: Hapus baris ini jika tim kamu mewajibkan lockfile (Disarankan tetap dicommit)

# ---------------------------------
# 2. BUILD OUTPUTS (React/Vite)
# ---------------------------------
dist/
dist-ssr/
build/
# Folder temporary Vite PWA (Sangat penting dihapus)
dev-dist/
*.local

# ---------------------------------
# 3. ENVIRONMENT & SECURITY (CRITICAL)
# ---------------------------------
# Jangan pernah commit file ini karena berisi API Key rahasia
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*

# ---------------------------------
# 4. CAPACITOR & ANDROID
# ---------------------------------
# Folder build Gradle
android/.gradle/
android/build/
android/app/build/

# File konfigurasi lokal (SDK Path tiap komputer beda)
android/local.properties

# Android Studio generated files
android/.idea/
android/app/*.iml
android/*.iml

# 🚨 PENTING: Mencegah duplikasi kode web di dalam folder Android
# Capacitor menyalin isi folder 'dist' ke sini. Kita tidak mau commit file hasil copy ini.
android/app/src/main/assets/public/
# Kecuali file konfigurasi Capacitor (jika ada)
!android/app/src/main/assets/public/capacitor.config.json

# Capacitor native plugins cache
android/capacitor.settings.gradle
android/capacitor-cordova-android-plugins/

# ---------------------------------
# 5. FIREBASE
# ---------------------------------
.firebase/
firebase-debug.log
firebase-debug.log*

# ---------------------------------
# 6. EDITOR & OS JUNK
# ---------------------------------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json # Opsional: jika ingin share setting editor tim
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

# ---------------------------------
# 7. TESTING & COVERAGE
# ---------------------------------
coverage/
test-results/

# ---------------------------------
# 8. DOKUMENTASI SEMENTARA (Opsional)
# ---------------------------------
# Jika kamu tidak ingin commit laporan progress harian
docs/archive/
*_REPORT.md
*_CHECKLIST.md